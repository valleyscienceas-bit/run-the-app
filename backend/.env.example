# AI Configuration
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# Email Configuration (SMTP)
SMTP_SERVICE=
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=

# Firebase Admin (path to service account JSON, or leave blank to use Application Default Credentials)
GOOGLE_APPLICATION_CREDENTIALS=

# Server
PORT=3001
NODE_ENV=development
