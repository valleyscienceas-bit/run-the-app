# Prompt queue

Scratch pad for xprompts aimed at different branches. Edit here instead of parking text on the clipboard.

**How to use**

1. Write the prompt under the right section.
2. Checkout that branch (`git checkout …`).
3. Open/continue a Cursor chat on that checkout.
4. `@docs/prompt-queue.md` or paste that section into the chat.
5. Clear or strike through the prompt when done.

Hub folder can only be on one branch at a time. For true parallel work, use git worktrees (see [branch-workflow.md](./branch-workflow.md)).

---

## Hub / AI — `feature/ai-functions-frontend-backend`

---

## UI — `feature/ui`

---



## CI — `feature/app-ci-and-tests`

When creating parent account, make sure password matches the requirements as if I was creating a new account.  

For 2-factor auth, make the name easier to understand; like Valley Science and user name or something along those lines. every time I use 2-factor auth or don't have it and still log in, it sends another parent creation email; fix that.   

backend has been doing some weird stuff; look at its entirety and review any bugs

Btw, it sent 2 emails to the parent account to set up.

You can't just be able to mark the lab or topics complete. You have to complete them or have taken a test that allows you to skip them.

CI workflow for the hub branch is broken.

---



## Modules — `feature/modules`

Also, after step 5 of the 1st module, it stops working, fix this for all modules. Also, are the current modules in the sandbox folder alligned for 3rd NGISS standards or 8th grade?

---



## Other / one-off (`ui/…`, etc.)

Branch: Create a new ui/* branch for this: In the lab, stuff just feels a little small, especially the words (fix all modules for that). Also, in the labs for all modules run then all on your own, and see what is not looking great (ex. in the balanced forces lab, when you hit the goal in the experiment, it moves forward not looking if the student actually was able to observe) fix all modules for that. The animation between steps is a little chunky, make it nicer. 

Branch: